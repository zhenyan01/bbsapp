<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bbsapp">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
  
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bbsapp">

        <provider
            android:name="cn.bmob.v3.util.BmobContentProvider"
            android:authorities="com.example.bbsapp.BmobContentProvider">
        </provider>

        <activity android:name=".activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.SignUp" />
        <activity android:name=".activity.Switch" />
        <activity android:name=".activity.Questionnarie" />
        <activity android:name=".activity.Receive" />
        <activity android:name=".activity.CommunityIndex" />
        <activity android:name=".activity.CreateCommunity" />
        <activity android:name=".activity.CreatePost" />
        <activity android:name=".activity.CreateReply" />
        <activity android:name=".activity.Chatting" />
        <activity android:name=".activity.ChannelActivity" />
        <activity android:name=".activity.ChangePassword"/>


    </application>

</manifest>